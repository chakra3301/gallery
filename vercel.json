{
  "installCommand": "npm install",
  "buildCommand": "git lfs install && (git remote get-url origin || git remote add origin https://github.com/chakra3301/gallery.git) && git lfs pull || echo 'Warning: Git LFS pull failed, some assets may be missing' && npm run build"
}
