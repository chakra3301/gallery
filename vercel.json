{
  "installCommand": "npm install",
  "buildCommand": "git lfs install && git config --global lfs.batch false && git lfs fetch origin && git lfs checkout || echo 'Warning: Git LFS fetch failed, continuing build' && npm run build"
}
