{
  "installCommand": "npm install",
  "buildCommand": "git lfs install && git config lfs.url https://github.com/chakra3301/gallery.git/info/lfs && git lfs pull || echo 'LFS pull failed, continuing build' && npm run build"
}
